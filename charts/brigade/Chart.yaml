apiVersion: v1
description: Brigade provides event-driven scripting of Kubernetes pipelines.
name: brigade
version: 0.15.0
# Note that we use appVersion to get images, so make sure this is correct.
appVersion: v0.15.0-33-g9d8d645
